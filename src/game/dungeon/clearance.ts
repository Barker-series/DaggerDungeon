/** Shared ordinary-route clearance. Terrain corridors, bridge passages and
 * fold-carved permanent transit must agree rather than restoring a lower
 * ceiling at a layer boundary. Deliberately tight pipes/vents are separate
 * authored profiles and do not use this value. */
export const TRANSIT_CLEARANCE = 6;
